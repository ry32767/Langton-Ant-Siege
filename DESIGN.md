# DESIGN.md — Langton Ant Siege のデザインアイデンティティ

> このファイルが**見た目の唯一の正**。色・書体・余白の値は `src/tokens.css` に固定してあり、
> すべてのコンポーネントはそこから導出する。ベタ書きの hex やマジックナンバーを混ぜないこと。
>
> **後続セッションへ**: デザインに手を入れる前にこのファイルを読むこと。ここに書いてある
> 「避けると決めた初期値」と「不変条件」を破ると、積み上げてきた一貫性が一回でリセットされる。

## 題材(1文)

**一般化ラングトンのアリが方眼の上に敷く「ハイウェイ」を弾道として、上下に構えた2陣営が撃ち合うリアルタイム攻城ゲーム。**
この画面の唯一の仕事は、**盤面で今なにが起きているかを一目で読ませ、次の1手を選ばせること**。

プレイヤーは攻撃9種・妨害9種の札から選んで撃ち、飛来する敵弾を識別して迎撃する。
だから画面が最優先で伝えるべきは順に ①盤面の状況 ②いま撃てる札 ③飛来中の脅威。

## なぜこの見た目なのか

題材はセルオートマトン — **方眼の上で、状態が積み重なって構造が創発する**もの。
セルオートマトンは歴史的に**方眼紙に手で塗って**研究されてきた。だから画面の地は
「暗いダッシュボード」ではなく **方眼ノート**にする。トレイルは紙に乗ったインクとして読める。

陣営色は **藍(indigo)と朱(vermilion)** — 和のインク2色。題材(格子の上のインク)に根ざしていて、
かつ色相・明度の両方で離れているので、色覚特性によらず判別できる。

### 避けると決めた初期値(これをやったら差し戻し)

- ❌ **真っ黒〜濃紺の背景 + 蛍光シアン/オレンジのネオン** — 「AIが作ったダッシュボード」の
  最頻出パターン。v3.1 の旧UIがまさにこれだった(`#0a0e14` + `#4f8fe8`/`#e8794f`)
- ❌ グロー/ブルーム/ネオンシャドウでの強調
- ❌ Inter / システムsans をそのまま本文に使う
- ❌ Tailwind デフォルトの `gray-*` 相当の無彩色グレー(必ず紙側の暖色を少し乗せる)
- ❌ 角丸を大きく取ってカードを浮かせる「SaaS風」レイアウト

## トークン(実体は `src/tokens.css`)

### Color

| トークン | 値 | 用途 |
|---|---|---|
| `--paper` | `#F2EDE3` | 地。生成りの紙 |
| `--paper-sunk` | `#E8E1D4` | 一段沈んだ面(盤面の外側・無効化) |
| `--rule-fine` | `#DFD7C8` | 方眼の細線(1セルごと) |
| `--rule-major` | `#CBC0AC` | 方眼の太線(16セルごと。v5で10から変更) |
| `--sumi` | `#2A2620` | 本文の墨。紙に対し 13:1 |
| `--sumi-soft` | `#5C5548` | 補助テキスト。紙に対し 5.9:1 |
| `--ai` (藍) | `#1F3A6E` | **side1 = プレイヤー**。紙に対し 10.0:1 |
| `--hanada` (縹) | `#2C6E9B` | **side1 slot1** |
| `--sumire` (菫) | `#383894` | **side1 slot2** |
| `--seiheki` (青碧) | `#1B6B62` | **side1 slot3** |
| `--shu` (朱) | `#B3402B` | **side2 = 敵**。紙に対し 4.8:1 |
| `--daidai` (橙) | `#C2661B` | **side2 slot1** |
| `--kurenai` (紅) | `#7D2139` | **side2 slot2** |
| `--enji` (臙脂) | `#6E2A22` | **side2 slot3** |
| `--kin` | `#A67C1A` | 注意・トークン残量 |
| `--midori` | `#3F6B4A` | 成功・得点 |
| `--akane` | `#9C2F2F` | 危険・**今は撃てない札**の強調。沈んだ地(`--paper-sunk`)に対し 5.65:1 |

側の色は**必ず `--ai` / `--shu` から導出**する。生の hex を書かない。

### HUD の状態表示

盤面と HUD で、プレイヤーと敵が見るべき情報を冗長に示す。

- **得点** — `.pip` の5升。得点が入った瞬間、その升目に `pip-soak` キーフレーム(約180ms)が1回再生され、インクが染み込む様子を表現する
- **飛行中のアリ** — `.flying-pip` の4升。slot 0..3 に固定対応し、その slot のアリが飛んでいればそのアリの個体色で塗る。盤面の `#1`〜`#4` バッジ・軌跡の色と一致する
- **トークン** — `.token-bar` の連続バー。升目にすると30個でノイズになるため。5トークンごとに `--rule-major` の目盛りを入れて数えられるようにしてある。CSS 変数 `--fill` (0..1)を塗り幅に反映させる
- **数値テキスト** — 得点・トークン・飛行中アリ数とも、視覚表現に加えて数値テキストを**必ず併記**する。色や長さだけに意味を持たせない

HUD の折り返しをやめ、狭幅は2行・広幅は1行に詰めてある。ラベルは `あなた` / `CPU` だけにして陣営色と凡例で分かるようにしており、得点・トークン・飛行中アリの数値テキストは残している。

### 陣営とセル状態の同時表現(この設計の要)

盤面のセルは **状態 0..15**、**最後に触れた陣営**、**その陣営のアリ個体** の3つの情報を持つ。
256個の色を用意するのではなく、**3軸を直交させる**:

```
色相の「帯」 = どちらの陣営か        (寒色帯 or 暖色帯。最短104°離れている)
帯の中の位置 = どのアリか            (slot 0..3)
濃度         = セルの状態値 (0..15)  (薄いウォッシュ → 濃いインク)
```

状態0は「何も塗られていない」＝紙のまま。状態が上がるほどインクが濃くなる。
描画対象は状態 1..15 で、これを `--ink-alpha-min` (0.12) 〜 `--ink-alpha-max` (0.92) に線形補間する。

これにより **「どちらの弾がどこを通ったか」が一目で読める**(ユーザー要件)。
そして色数の違う弾どうしが衝突して状態が圧縮される様子も、濃度の変化として見える。

⚠️ 色だけに意味を持たせない: 個体は**色だけでなく `#1`〜`#4` のバッジ・自爆パネルのスウォッチ・選択時のハイライトでも冗長に示す**。陣営は**画面上の位置**(side1=上/side2=下)と HUD のラベル文字でも冗長に示す。アリの形も種別で変える(攻撃=三角、妨害=菱形)。

### Type

| ロール | 書体 | 用途 |
|---|---|---|
| display | `'Yu Mincho', 'Hiragino Mincho ProN', 'Noto Serif JP', serif` | 画面タイトル・結果表示。**ここだけ**明朝を使う |
| body | `'Hiragino Sans', 'Yu Gothic', 'Noto Sans JP', sans-serif` | 本文・ボタン・ラベル |
| numeric | `'Roboto Mono', 'SFMono-Regular', Consolas, monospace` | 得点・トークン・残り時間・座標。**tabular-nums 必須**(数字が動くとガタつくため) |

明朝を display にだけ使うのは、和のインクという題材に繋げるため。本文には使わない(可読性)。
本文は最小 16px(iOS の自動ズーム回避)。

### Layout & space

**中心概念: 方眼。** すべての間隔を 4px グリッドの倍数に乗せる。
盤面は 256×256 の**正方形(1:1、v5)**なので、レイアウトは:

- **広幅(≥900px)**: 盤面を中央に配置し、盤面の右に**ハンドル列**と**開いている区画を1つだけ**表示。盤面は縦を基準に高さいっぱい
- **狭幅(<900px)**: 1カラム。盤面が上、盤面のすぐ下に**ハンドル列**、その下に**開いている区画を1つだけ**表示。盤面は幅を基準に縮小

**対戦画面 `#screen-match` は `height: 100dvh; overflow: hidden` で、ページのスクロールを0にしている。** あふれる中身は開いている区画の内側だけを `overflow-y: auto` でスクロール可能にする。

ハンドル列は盤面の端に沿って常に画面内にあり、全区画を閉じた状態からでも操作に戻れる。区画は盤面の外側に開き、盤面に重ねない。

間隔スケール `--sp-1`..`--sp-6` = 4/8/12/20/32/48px。
角丸は `--radius` = 3px のみ(紙に引いた枠なので大きく丸めない)。影は使わず、境界は 1px の罫線で表す。

### Signature

**盤面そのものが方眼紙であること。** 罫線が実際に引かれていて、インクがその上に乗る。
配置可能帯は「紙を折った」ように `--paper-sunk` で沈ませ、折り目を `--rule-major` で1本引く。
他のどのゲームUIでもない、この作品だけの見え方をここに集中させ、周囲は静かに保つ。

得点ゲート(v5)も同じ語彙で表現する。ゲート外(左右32列ずつの「壁」)は `--rule-major` の
淡い塗り足しで沈め、ゲート境界は `--rule-major` の点線で示す。ネオンやグローは使わない。

発射列(antX。v5で可変化)の選択も同じ語彙で表現する。選択中のテンプレートが実際に
進む向きを `--ai` の細い破線で示す。ハイウェイは斜めに進むため縦の直線では嘘になるので、
攻撃テンプレートは発射位置から予測到達列(UIが数字で出す値と必ず一致する)まで、
妨害テンプレートは `highway.driftX/driftY` の傾きぶんの短いガイドを描く。盤面は左右が
トーラスなので、進路が盤面端をまたぐ場合は線分に分割して描く。進行方向は線の途中に
添える小さな矢印1つでも示す(盤面の情報表示なので「UIを盤面に重ねない」不変条件の
例外にはあたらない)。太さ・不透明度とも控えめにし、ネオンやグローにしない。

### モーション

| トークン | 値 | 用途 |
|---|---|---|
| `--dur-score-line-flash` | `250ms` | 得点が入ると、盤面の得点ライン(敵陣の折り目)がその陣営の色で一瞬濃くなる時間。光らせない |
| `--dur-pip-soak` | `180ms` | HUD の得点升目がインクを吸い込むキーフレーム時間。`style.css` で定義 |
| `--dur-self-destruct-fx` | `300ms` | 自爆時に、そのアリの軌跡を個体色で alpha 減衰させながら重ね描きし、細い輪が広がって消える時間。canvas 描画 |

演出は**紙とインクの比喩に収める**。グロー・ブルーム・ネオンシャドウは使わない。

ユーザーが `prefers-reduced-motion: reduce` を設定していれば、**CSS 層と Canvas 層の演出をともに無効にする**。`src/tokens.css` で `--dur-fast: 0ms` に、`src/app.js` の `prefersReducedMotion()` を参照して Canvas アニメーションをスキップする。

## 不変条件(壊してはいけない)

1. **盤面の縦横比 1:1 (256列×256行) を必ず維持する。**(v5で1:2・60×120から変更) 引き伸ばさない。`aspect-ratio` で固定する
2. **UI要素が盤面に重ならない。** 結果オーバーレイだけが例外(それが仕事なので)
3. **陣営の色は入れ替えない。** side1=藍=画面上、side2=朱=画面下。プレイヤーは常に side1
4. **陣営は「色相の帯」で分ける。個体色は帯の中だけで振る。** 寒色帯=side1 / 暖色帯=side2 で、帯どうしは90°以上離すこと。個体色を増やすときは必ず `scratchpad/check-bands.mjs` 相当の色相差チェックを通し、帯をまたがせない。未割り当てのセルは `lastToucherSide` を見て必ずその陣営の slot0 に倒す — 個体の割り当てが分からなくなっても陣営だけは絶対に正しくする
5. **状態の濃度ランプは単調。** 状態値が大きいほど濃い。逆転させない
6. **色だけで状態を表さない。** 陣営は位置とラベルでも、アリ種別は形でも示す
7. **本文16px以上・タップ領域44×44px以上**を下回らない
8. **数値表示は tabular-nums。** 毎フレーム更新される値がガタつくのを防ぐ
9. **盤面を囲む要素に `max-height` と `justify-content: center` を同時に掛けない。** 子の合計が上限を超えると中央寄せは**上下両方**にあふれ、上にある HUD へ静かに重なる(実測 21px、不変条件2違反)。高さの上限は `.battle-area` だけが持ち、列は `flex-start` で上詰めにする
10. **盤面の一辺は `min()` で決め、`aspect-ratio` に守らせる。** `height` を固定して別に `width`/`max-width` を与えると `aspect-ratio` が負けて引き伸ばされる(実測 530×714、不変条件1違反)。`.battle-area` が `width: min(100%, var(--board-max-h))` ＋ `aspect-ratio` で一辺を決め、`#board` は `width/height:100%` で満たすだけにする
11. **ページをスクロールさせない。** 対戦画面は `height: 100dvh; overflow: hidden`。あふれる中身は開いている区画の内側だけを `overflow-y: auto` でスクロールさせる。レイアウトを変えたら 375×667 / 375×812 / 1280×900 の3サイズで `scrollHeight - clientHeight === 0` を実測して確かめる
12. **同時に開く区画は1つだけ。閉じるときは `display:none` にする。** スクロールで隠すだけでは高さが戻らず、ページのスクロールが復活する
13. **ハンドル列は常に画面内に置く。** 全区画を閉じた状態から操作に戻れなくなるため。区画は盤面の**外側**に開き、盤面には重ねない(不変条件2)。「盤面の端」に置くのはハンドルであって区画ではない

## 対象画面(このリデザインで刷新するもの)

| 画面 | 扱い |
|---|---|
| 開始画面 `#screen-start` | 刷新する |
| 対戦画面 `#screen-match`(HUD・盤面・操作パネル・脅威パネル) | 刷新する |
| 結果オーバーレイ `#result-overlay` | 刷新する |

発射列選択(v5): テンプレート選択中(PvC のみ)、盤面のクリック/ドラッグまたは
盤面・操作パネルへのフォーカス中の矢印キー(←/→で1列、Shift+←/→で16列)で
発射列 `antX` を変えられる。座標変換は自前で書かず `src/template.js` の
`instantiateTemplate` / `scoringLaunchColumns` / `launchColumnScores` / `wrapX` を経由する。

**触らないもの**: `src/engine.js` / `src/cpu.js` / `src/search/*` / `scripts/*` / `data/*`。
デザイン変更でゲームロジックに手を入れない。

## 既存機能インベントリ(リグレッションチェックリスト)

見た目を作り直しても、以下は**1つも失わない**。刷新後に1項目ずつ実際に操作して確認する。

- [ ] 「プレイヤー vs CPU」で開始できる
- [ ] 「CPU vs CPU」で開始できる
- [ ] データ読み込み完了までボタンが無効、失敗時にエラーが出る
- [ ] 攻撃/妨害タブを切り替えられる(切替で選択がリセットされる)
- [ ] テンプレートカードを選択・再クリックで解除できる
- [ ] トークン不足・条件未達のカードは選べない(無効表示)
- [ ] 発射ボタンで選択中テンプレートを撃てる
- [ ] 発射できない理由がテキストで出る(kind/cells/zone/flying/tokens/未選択)
- [ ] 飛来中の敵攻撃アリが脅威一覧に出て、識別ID が表示される
- [ ] 脅威を選ぶと、有効な妨害テンプレートが強調され、推奨タイミングが出る
- [ ] 選択中テンプレートのプレビューが盤面に重畳表示される(PvC のみ)
- [ ] HUD に 双方の得点 / 自分のトークン / 飛行中アリ数 / 残り時間 が出る
- [ ] 試合終了で結果オーバーレイが出る(勝敗・最終スコア・試合時間)
- [ ] 「もう一度対戦する」で同モード再開
- [ ] 「タイトルに戻る」で開始画面へ
- [ ] CPU vs CPU では操作パネルが出ない
- [ ] 盤面クリック/タップ・ドラッグ・キーボード(←/→、Shiftで16列)で発射列を選べる(PvC のみ)
- [ ] 攻撃を選ぶと、実際に進む斜めの進路ガイドと進行方向の矢印が出て、その終点が「予測到達列(x=NNN)」の数字と一致する
- [ ] 進路が盤面の左右端をまたぐときは、逆走する1本線ではなく線分に分割して描かれる
- [ ] 妨害を選ぶと、初期の向きの矢印が出る(`reachRows > 0` ならその深さに破線も出る。横方向のドリフトは未知なので線は引かない)
- [ ] 「発射」ボタンは札の区画を開いている間つねに画面内にあり、盤面に重ならない(ページはスクロールしないので「スクロールすれば届く」は不可)
- [ ] 撃てる札は左端の藍のインクの筋で一目で分かる
- [ ] 得点ゲートを外れる発射列を選ぶと警告テキストが出る(発射自体は禁止しない)
- [ ] 得点ゲートの警告は攻撃タブでだけ出る(妨害は敵陣に届かない設計なので出ない)
- [ ] 脅威を選ぶと、迎撃候補に「どの列から・いつ撃つか」(x=NNN)が出る
- [ ] 脅威を選んだ状態で妨害テンプレートを選ぶと、発射列が自動でその「当たる列」に寄る
- [ ] 軌跡がアリごとに色分けされ、同じ陣営の複数のアリを区別できる
- [ ] どの個体色も相手陣営の帯には入らない(寒色＝自分 / 暖色＝敵 が常に成立する)
- [ ] 自爆パネルの行の色・バッジと、盤面のアリ・その軌跡の色が一致する
- [ ] 盤面のバッジが震えずに読める(アリ本体は現在地、バッジはならした位置に描く)
- [ ] 得点が入ると、盤面の得点ラインとHUDの升目の両方で分かる
- [ ] 自爆すると、そのアリの軌跡が消えていく様子と輪の演出が出る
- [ ] HUD の飛行中の升目の色が、盤面のアリ・軌跡の個体色と一致する
- [ ] テンプレートカードでコストが最初に、いちばん目立つ形で読める
- [ ] ハンドル(攻撃/妨害/脅威/自軍/凡例)を押すと、その区画だけが開く
- [ ] 「隠す」で盤面だけになり、「表示」で直前に開いていた区画に戻る
- [ ] 全区画を閉じてもハンドル列は画面内にあり、操作に戻れる
- [ ] 撃てない札が赤(`--akane`)で強調され、「あと N」で不足トークンが分かる

## 変更履歴

| 日付 | 変更 |
|---|---|
| 2026-08-07 | 初版。v3.1 の暗色ダッシュボードUIから方眼紙+和インクへ全面刷新。盤面の90度回転(120×60 横 → 60×120 縦)に伴うレイアウト再構成と、セル状態0..15 × 陣営2 の同時表現を定義 |
| 2026-08-08 | v5: 盤面を 60×120(1:2) → 256×256(1:1)に拡大。`drawBoard` を per-cell fillRect から ImageData 拡大描画に変更、太線間隔を10→16に変更、得点ゲート(壁+境界点線)の描画を追加。不変条件1を1:1に更新 |
| 2026-08-08 | v5: 発射列(antX)の選択操作を追加。`drawPreview` を相対 cells → `instantiateTemplate` 経由の描画に修正、盤面クリック/ドラッグ/キーボードで発射列を選べるようにし、縦のガイド線・予測到達列・得点ゲート外警告を追加 |
| 2026-08-08 | 盤面バッジ(`#1`〜`#4`・敵の識別ID)の位置を、アリの現在地から**指数移動平均でならした追従位置**に変更。アリは120ステップ/秒で毎ステップ曲がるため現在地に貼ると震えて読めなかった。左右トーラスの巻き戻りは差分を短い方の向きで測って回避。slot ごとに段をずらしてバッジ同士の重なりも防ぐ |
| 2026-08-08 | 軌跡をアリ個体ごとに色分け。陣営を単色から「色相の帯」に拡張し(寒色帯=side1 / 暖色帯=side2、最短104°)、帯の中の slot 0..3 で個体を示すようにした。未割り当てセルは lastToucherSide で陣営 slot0 にフォールバックする |
| 2026-08-08 | HUD に飛行中の升目(slot色)とトークンの連続バーを追加。得点時に得点ラインとHUD升目、自爆時に軌跡のフェードと輪の演出を追加(いずれも `prefers-reduced-motion` を尊重)。テンプレートカードをコスト優先の情報階層に変更。あわせて盤面とHUDの重なり・盤面の引き伸ばし・カード内容のはみ出しを修正 |
| 2026-08-08 | 発射列ガイドを縦の直線から予測進路(斜め)に修正。ハイウェイは斜めに進むため縦線では進行方向を誤って示していた。攻撃は予測到達列まで、妨害は傾きぶんの短いガイドを描き、盤面端をまたぐ場合は線分に分割、進行方向の矢印を1つ添えた |
| 2026-08-08 | 操作UIを「盤面の端のハンドル列＋一度に1つだけ開く区画」に再構成し、対戦画面のページスクロールを0にした(375×667/375×812/1280×900 で実測)。HUD の折り返しをやめて 209px→47px(狭幅)・169px→35px(広幅)に圧縮。凡例を区画へ移動。撃てない札を `--akane` で強調。札一覧を3列グリッドにして3種化に備えた |

## 過去に踏んだレイアウトの罠

**盤面とカード内容のはみ出し** — `.template-list` に `max-height: 40vh` があるため grid の `auto` 行がその高さに押し込められ、必要 123px の中身(コスト・カウンター・ルール)が 67px の行に潰れて、「コスト」行がカードの外へはみ出し、隣のカードに重なっていた。ユーザーには「コストが書かれていない」と見えていた。→ `grid-auto-rows: min-content` と `align-content: start` を入れ、長いルール文字列用に `overflow-wrap: anywhere` で自動折り返しするようにした。

**盤面と HUD のレイアウト変更への追従** — `setupCanvas()` が window の `resize` と試合開始時の呼び出しにしか反応していなかったため、ブラウザペイン表示/非表示の切り替えやスクロールバー出現による数 px のズレを取りこぼしていた。→ `ResizeObserver` で盤面 canvas の実寸を監視し、変化したフレームで再描画するようにした。

盤面そのものの引き伸ばしと HUD への重なりは、不変条件9・10 で規律を定めた。

## 既知の残課題

- v5 で盤面が 1:1 の正方形になったため、縦長(landscape で盤面が小さくなる)問題は構造的に
  解消した。収まり方は 375×667 / 375×812 / 1280×900 の3サイズで実測済み(全区画の状態で
  スクロール量0・盤面は正方形を維持)。**実機の実端末では未確認。**
- **Canvas の描画は未検証。** 得点時の得点ライン強調、自爆時の軌跡フェードと輪、発射列の
  進路ガイドと矢印は、いずれも実装済みだが**実際の見え方を目で確認できていない**
  (検証環境でブラウザのペインが表示されず `requestAnimationFrame` が止まるため、盤面が
  1フレームも描画されない)。進路ガイドだけは、線分分割の計算を `src/template.js` の
  純粋関数に切り出して単体テスト7本＋実データ54通りで「終点が予測到達列と一致する」ことを
  確かめてあるが、**線の見た目そのものは未確認**。ペインを表示できる環境で目視すること。
